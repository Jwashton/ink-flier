interface SceneObject {
  id: number;
  name: string;

  // program
  // transform
  // state

  // children?
  // parent?

  // mesh?
  // material?
  // texture?
}

const sceneObject: SceneObject = {
  id: 0,
  name: 'SceneObject',
};

export default sceneObject;
